# Minimal example

Give every member the same frozen subject and snapshot. The Leader assigns independent governance, judicial, and operations tasks. The Reviewer accepts supported results, returns a conflict issue for incompatible employee-count evidence, and requests only an employee-count repair from `operations-peer-agent`.
